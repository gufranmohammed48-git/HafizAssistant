[phases.setup]
aptPkgs = ["...", "build-essential", "gcc", "g++", "ffmpeg", "libsndfile1", "git", "python3-dev"]

[phases.install]
cmds = [
  "pip install --no-cache-dir --upgrade pip cython setuptools wheel",
  "pip install --no-cache-dir --no-build-isolation -r requirements.txt"
]
